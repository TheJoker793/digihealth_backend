services:

  # ══════════════════════════════════════════════
  # INFRASTRUCTURE
  # ══════════════════════════════════════════════

  cim11-api:
    image: whoicd/icd-api
    container_name: cim11-api
    environment:
      - acceptLicense=true
      - saveAnalytics=false
      - include=2026-01_en
    ports:
      - "5008:80"
    networks:
      - digihealth-net
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:management-alpine
    container_name: digihealth-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - digihealth-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ══════════════════════════════════════════════
  # MICROSERVICES
  # ══════════════════════════════════════════════

  auth-service:
    build:
      context: ./Auth-microservice
      dockerfile: Dockerfile
    container_name: digihealth-auth
    ports:
      - "5202:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
      cim11-api:
        condition: service_started
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      CIM11__BaseUrl: http://cim11-api:80
      CIM11__ApiVersion: v2
      CIM11__Language: en
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  patient-service:
    build:
      context: ./Patient-microservice
      dockerfile: Dockerfile
    container_name: digihealth-patient
    ports:
      - "5100:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_patient;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  rendezvous-service:
    build:
      context: ./Rendez_Vous-microservice
      dockerfile: Dockerfile
    container_name: digihealth-rendezvous
    ports:
      - "5101:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_rv;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  dossier-medical-service:
    build:
      context: ./Dossier_Medical-microservice
      dockerfile: Dockerfile
    container_name: digihealth-dossierMedical
    ports:
      - "5102:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_dossierMedical;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  prescription-service:
    build:
      context: ./Prescription-microservice
      dockerfile: Dockerfile
    container_name: digihealth-prescription
    ports:
      - "5103:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_prescription;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  facturation-service:
    build:
      context: ./Facturation-microservice
      dockerfile: Dockerfile
    container_name: digihealth-facturation
    ports:
      - "5104:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_facturation;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  document-service:
    build:
      context: ./Document-microservice
      dockerfile: Dockerfile
    container_name: digihealth-document
    ports:
      - "5105:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_document;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  notification-service:
    build:
      context: .
      dockerfile: Notification-microservice/Dockerfile
    container_name: digihealth-notification
    ports:
      - "5106:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_notification;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  statistique-service:
    build:
      context: .
      dockerfile: Statistique-microservice/Dockerfile
    container_name: digihealth-statistique
    ports:
      - "5107:80"
    networks:
      - digihealth-net
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=digihealth_statistique;User ID=sa;Password=DigiHealth2024!;Encrypt=True;TrustServerCertificate=True;"

  # ══════════════════════════════════════════════
  # API GATEWAY
  # ══════════════════════════════════════════════

  api-gateway:
    build:
      context: .
      dockerfile: API_Gateway-microservice/Dockerfile
    container_name: digihealth-gateway
    ports:
      - "5140:80"
    volumes:
      - ./API_Gateway-microservice/Ocelot.json:/app/Ocelot.json:ro
    networks:
      - digihealth-net
    restart: always
    environment:
      ASPNETCORE_URLS: http://+:80
      ASPNETCORE_ENVIRONMENT: Development

networks:
  digihealth-net:
    driver: bridge